# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Editor / IDE (personal workflow config; keep local)
.vscode/

# Experiment artifacts (run logs + checkpoints live under runs/<exp>/ — see scripts/dispatch_rtfm.sh)
runs/
experiments/runs/
*.ckpt
*.pt
*.log
events.out.tfevents.*

# Knowledge base raw source binaries (registered in sources/SOURCES.md)
knowledge/sources/files/
# leftover personal machine alias; remote dispatch retired — safe to delete
.env.remote
.publish-check.local

# Local-only, never-committed working context.
.local/
