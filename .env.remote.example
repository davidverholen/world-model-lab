# Remote GPU box for scripts/remote.sh — copy to .env.remote (gitignored) and edit.
# Prefer an ~/.ssh/config alias so host/user/key never appear in the repo.
WM_REMOTE=wm-desktop
WM_REMOTE_DIR=world-model
